import React from "react";
import LoginPopup from "../components/popups/LoginPopup";

const LoginPage = () => {
  return (
    <div>
      <LoginPopup mode="page" />
    </div>
  );
};

export default LoginPage;
